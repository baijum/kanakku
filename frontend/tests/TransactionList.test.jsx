import React from \'react\';\nimport { render, screen } from \'@testing-library/react\';\nimport \'@testing-library/jest-dom/extend-expect\';\nimport TransactionList from \'../src/components/Transactions/TransactionList\'; // Adjust path if needed\n\n// Mock transaction data\nconst mockTransactions = [\n  {\n    id: 1,\n    date: \'2024-01-15\',\n    description: \'Coffee Shop\',\n    amount: -5.50,\n    postings: [\n      { account: \'Expenses:Food\', amount: 5.50 },\n      { account: \'Assets:Checking\', amount: -5.50 },\n    ]\n  },\n  {\n    id: 2,\n    date: \'2024-01-16\',\n    description: \'Salary Deposit\',\n    amount: 2000.00,\n    postings: [\n      { account: \'Income:Salary\', amount: -2000.00 },\n      { account: \'Assets:Checking\', amount: 2000.00 },\n    ]\n  },\n];\n\ndescribe(\'TransactionList Component\', () => {\n  test(\'renders message when no transactions are provided\', () => {\n    render(<TransactionList transactions={[]} />);\n    // Adjust the expected text based on your component\'s implementation\n    expect(screen.getByText(/no transactions available/i)).toBeInTheDocument();\n  });\n\n  test(\'renders a list of transactions correctly\', () => {\n    render(<TransactionList transactions={mockTransactions} />);\n\n    // Check if the descriptions of both transactions are rendered\n    expect(screen.getByText(\'Coffee Shop\')).toBeInTheDocument();\n    expect(screen.getByText(\'Salary Deposit\')).toBeInTheDocument();\n\n    // Check if amounts are rendered (consider formatting)\n    expect(screen.getByText(/\-5\.50/)).toBeInTheDocument(); // Use regex for potential formatting\n    expect(screen.getByText(/2,?000\.00/)).toBeInTheDocument(); // Allow for commas\n\n    // Check if dates are rendered (consider formatting)\n    expect(screen.getByText(\'2024-01-15\')).toBeInTheDocument();\n    expect(screen.getByText(\'2024-01-16\')).toBeInTheDocument();\n\n    // Verify the number of transaction items rendered (optional, depends on structure)\n    // Example: Assuming each transaction is in a list item\n    const listItems = screen.getAllByRole(\'listitem\'); // Adjust role based on your HTML structure\n    expect(listItems).toHaveLength(mockTransactions.length);\n  });\n\n  // Add more tests here if the component has features like:\n  // - Clicking a transaction to view details\n  // - Sorting transactions\n  // - Filtering transactions\n  // - Delete/Edit buttons (would require mocking API calls)\n}); 